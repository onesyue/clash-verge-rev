#!/bin/bash
set -euo pipefail

# Load Rust/Cargo if installed via rustup
[ -f "$HOME/.cargo/env" ] && source "$HOME/.cargo/env"

if ! command -v "cargo-make" >/dev/null 2>&1; then
  echo "❌ cargo-make is required for pre-push checks."
  cargo install --force cargo-make
fi

cargo make pre-push
